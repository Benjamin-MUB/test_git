#!/bin/groovy

pipeline {
    agent any
    stages {
        stage('begin')
            {
                echo 'hello world'
            }
        }
    post {
        always {
            echo 'finish'
            }
    }
}
